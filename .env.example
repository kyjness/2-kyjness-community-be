# 서버 설정
HOST=0.0.0.0
PORT=8000
DEBUG=True

# CORS 설정
CORS_ORIGINS=*

# 세션 설정
SESSION_EXPIRY_TIME=86400

# Rate Limiting 설정
RATE_LIMIT_WINDOW=60
RATE_LIMIT_MAX_REQUESTS=10

# 파일 업로드 설정
MAX_FILE_SIZE=10485760
ALLOWED_IMAGE_TYPES=image/jpeg,image/jpg,image/png

# API 기본 URL (파일 업로드 URL 생성용)
BE_API_URL={BE-API-URL}
