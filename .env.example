# ========== 환경 변수 견본 (복사 후 .env 로 저장하고 값 채우기. .env는 git에 올리지 않음) ==========

# MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=puppytalk

# 서버
HOST=0.0.0.0
PORT=8000
DEBUG=True

# CORS: 프론트 주소 쉼표 구분 (credentials 사용 시 * 불가)
CORS_ORIGINS=http://localhost:5500,http://127.0.0.1:5500

# 세션
SESSION_EXPIRY_TIME=86400

# Rate limiting
RATE_LIMIT_WINDOW=60
RATE_LIMIT_MAX_REQUESTS=100

# 파일 업로드
MAX_FILE_SIZE=10485760
ALLOWED_IMAGE_TYPES=image/jpeg,image/jpg,image/png

# API 기본 URL (로컬: http://localhost:8000, 배포 시 실제 URL)
BE_API_URL=http://localhost:8000

# 파일 저장소: local | s3 (배포 시 s3 권장)
STORAGE_BACKEND=local
S3_BUCKET_NAME=
AWS_REGION=ap-northeast-2
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
S3_PUBLIC_BASE_URL=
