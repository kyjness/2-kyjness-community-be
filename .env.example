# ========== 환경 변수 견본 (복사 후 .env 로 저장하고 값 채우기. .env는 git에 올리지 않음) ==========
# 배포 시 반드시 수정할 항목: DEBUG, CORS_ORIGINS, BE_API_URL, (S3 사용 시) STORAGE_BACKEND, S3_*

# MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=puppytalk

# 서버
HOST=0.0.0.0
PORT=8000
# [배포 시 수정] 배포 시 false 로 설정
DEBUG=True

# [배포 시 수정] 프론트 주소 쉼표 구분 (credentials 사용 시 * 불가)
CORS_ORIGINS=http://localhost:5500,http://127.0.0.1:5500

# 세션
SESSION_EXPIRY_TIME=86400

# 로깅 (레벨: DEBUG, INFO, WARNING, ERROR / 파일 비우면 콘솔만, 설정 시 로테이팅 파일 추가)
LOG_LEVEL=INFO
LOG_FILE_PATH=

# Rate limiting
RATE_LIMIT_WINDOW=60
RATE_LIMIT_MAX_REQUESTS=100

# 파일 업로드
MAX_FILE_SIZE=10485760
MAX_VIDEO_SIZE=52428800
ALLOWED_IMAGE_TYPES=image/jpeg,image/jpg,image/png
ALLOWED_VIDEO_TYPES=video/mp4,video/webm

# [배포 시 수정] API 기본 URL (이미지 등 링크 생성에 사용)
BE_API_URL=http://localhost:8000

# [배포 시 수정] 파일 저장소 (배포 시 s3 권장)
STORAGE_BACKEND=local
S3_BUCKET_NAME=s3 사용 시 버킷 이름
AWS_REGION=ap-northeast-2
AWS_ACCESS_KEY_ID=s3 사용 시 IAM 액세스 키
AWS_SECRET_ACCESS_KEY=s3 사용 시 IAM 시크릿 키
# S3 공개 URL 접두사 (선택, CloudFront 쓰면 여기 설정)
S3_PUBLIC_BASE_URL=CloudFront 등 공개 URL 접두사
